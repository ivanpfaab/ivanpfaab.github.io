export const metadata = {
  title: "Hello, World",
  date: "2026-01-10",
  excerpt:
    "A placeholder post proving the on-site MDX rendering pipeline works end-to-end.",
};

This is a placeholder post proving the on-site MDX pipeline works. Replace this
file, or add new ones, under `content/writing/` — every `.mdx` file there
automatically becomes a post, no code changes required.

The post title comes from `metadata.title` and is already rendered by the page
template above, so don't repeat it as a heading here — start the body content
directly, like this file does.

## What you can do here

- Write **Markdown** as usual.
- Use `inline code` and fenced code blocks.
- Import and use React components if you need something more custom.
